# Copy this file to Appweb/.env and fill in your values.
# The shared/database.php reads these via getenv().
# Never commit .env to version control.

DB_HOST=127.0.0.1
DB_NAME=cephradb
DB_USER=root
DB_PASSWORD=
